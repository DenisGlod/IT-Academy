<?xml version = "1.0" encoding = "utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <property name="connection.url">jdbc:postgresql://localhost:5432/free_book_hb</property> <!-- BD Mane -->
        <property name="connection.driver_class">org.postgresql.Driver</property> <!-- DB Driver -->
        <property name="connection.username">user</property> <!-- DB User -->
        <property name="connection.password">zxCVqw</property> <!-- DB Password -->

        <property name="current_session_context_class">thread</property>

        <property name="dialect">org.hibernate.dialect.PostgreSQL10Dialect</property> <!-- DB Dialect -->
        <property name="hbm2ddl.auto">validate</property> <!-- create / create-drop / update / validate / drop-->
        <property name="hbm2ddl.import_files">data.sql</property>

        <property name="hibernate.default_catalog">free_book_hb</property>
        <property name="hibernate.default_schema">public</property>

        <property name="show_sql">true</property> <!-- Show SQL in console -->
        <property name="format_sql">true</property> <!-- Show SQL formatted -->

        <!-- c3pO connection -->
        <property name="c3p0.min_size">10</property>
        <property name="c3p0.max_size">100</property>
        <property name="c3p0.timeout">300</property>
        <property name="c3p0.acquire_increment">50</property>
        <property name="c3p0.idle_test_period">3000</property>

        <!-- Mapping -->
        <mapping class="by.freebook.dao.entity.UserData"/>
        <mapping class="by.freebook.dao.entity.Role"/>
        <mapping class="by.freebook.dao.entity.User"/>
        <mapping class="by.freebook.dao.entity.Book"/>
    </session-factory>
</hibernate-configuration>